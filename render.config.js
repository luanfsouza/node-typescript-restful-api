// eslint-disable-next-line @typescript-eslint/no-var-requires
import { init } from "render";

const config = {
  projectId: "SEU_ID_DO_PROJETO",
  projectKey: "SEU_CHAVE_DO_PROJETO",
  apiHost: "https://api.render.com",
};

init(config);
